# Workspace configuration fixtures

These files are inputs for validator and setup tests. Copy one fixture to `.product-os/config.yaml` in a temporary workspace and validate it against the canonical `schemas/config.schema.json`.

They deliberately do not copy schemas, templates, skills, or adapters. The repository root remains the only canonical source.
