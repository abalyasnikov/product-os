schema_version: 1
workspace_profile: core_workspace
selected_client: codex
default_branch: main
review:
  mode: provider
  approver_rule:
    initiative: <configured-manager-or-team>
    prd: <configured-manager-or-team>
  solo_approval:
    allowed: false
    commit_trailer: "Product-Approval: explicit"
  git_capability: git.review.read
evidence:
  storage: reference_only
  max_excerpt_chars: 500
connectors: {}
